[package]
name = "wyrm-gui"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT"
repository = "https://github.com/1ARdotNO/wyrm"
authors = ["1ARdotNO"]
description = "Native GUI editor for wyrm OTM threat models"

[dependencies]
otm-core = { path = "../otm-core" }
# Pinned to 0.29 — egui 0.36 is a major API break (App::ui, panel moves,
# CornerRadius). Renovate is held here (see renovate.json) until we migrate.
eframe = "~0.29"
egui = "~0.29"
