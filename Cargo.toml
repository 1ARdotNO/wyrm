[workspace]
resolver = "3"
members = ["crates/otm-core", "crates/wyrm-cli", "crates/wyrm-lsp"]
# The GUI pulls a heavy native-windowing tree (eframe/winit/wgpu) with its own
# system-lib and license surface; keep it out of the core workspace so the main
# build/test/deny gates stay lean. It builds standalone (own Cargo.lock) + a
# dedicated CI job.
exclude = ["crates/wyrm-gui"]

[workspace.package]
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "MIT"
repository = "https://github.com/1ARdotNO/wyrm"
authors = ["1ARdotNO"]

[workspace.dependencies]
otm-core = { path = "crates/otm-core", version = "0.1.0" }
serde = { version = "1.0.229", features = ["derive"] }
serde_json = "1.0.151"
# Maintained fork of the archived serde_yaml — keeps the cargo-deny advisory gate green.
serde_yaml_ng = "0.10"
thiserror = "2.0.20"
clap = { version = "4.6.7", features = ["derive"] }
lsp-server = "0.10.0"
lsp-types = "0.97"

[profile.release]
strip = true
lto = "thin"
